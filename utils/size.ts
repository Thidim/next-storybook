import { TSize } from "@/types/common";
import { SizeProp } from "@fortawesome/fontawesome-svg-core";

export const sizeToIconSizeMap: Record<TSize, SizeProp> = {
  "x-small": "xs",
  small: "sm",
  regular: "sm",
  large: "sm",
  "x-large": "xl",
};
